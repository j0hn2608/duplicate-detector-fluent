{
    "name": "duplicate-detector-fluent",
    "version": "1.0.0",
    "description": "Finds duplicate records on any table and opens them in a dedicated workspace.",
    "license": "UNLICENSED",
    "imports": {
        "#now:*": "./@types/servicenow/fluent/*/index.js"
    },
    "scripts": {
        "build": "now-sdk build",
        "deploy": "now-sdk install",
        "transform": "now-sdk transform",
        "types": "now-sdk dependencies"
    },
    "devDependencies": {
        "@servicenow/sdk": "4.8.1",
        "@servicenow/glide": "27.0.5"
    }
}
