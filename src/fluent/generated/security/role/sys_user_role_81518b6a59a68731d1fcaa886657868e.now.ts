import { Role } from '@servicenow/sdk/core'

Role({
    name: 'x_916323_duplicate.duplicate_viewer',
    description: 'Grants access to the Duplicate Detector workspace and the duplicate-search data broker.',
})
